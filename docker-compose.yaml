version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: chatops-db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app:
    build: .
    container_name: chatops-app
    restart: always
    environment:
      DB_HOST: db
      DB_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_NAME}

      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      PROMETHEUS_URL: ${PROMETHEUS_URL}
      ALERTMANAGER_URL: ${ALERTMANAGER_URL}
      GPT_KEY: ${GPT_KEY}
      GPT_CATALOG: ${GPT_CATALOG}

      # Переменные для доступа к кластеру Kubernetes
      # Укажите здесь имя и зону вашего кластера
      K8S_CLUSTER_NAME: "your-cluster-name"
      K8S_CLUSTER_ZONE: "your-cluster-zone"

    depends_on:
      - db

volumes:
  postgres_data: {}
